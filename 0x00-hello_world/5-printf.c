#include <stdio.h>
/**
 * main - prints the phras with proper grammer,
 * but the outcome is a piece of art
 * Return: 0 if exited well
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
