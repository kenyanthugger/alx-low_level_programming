#include "main.h"
/**
 * _abs - compute absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

